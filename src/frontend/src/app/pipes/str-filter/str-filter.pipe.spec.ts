import { StrFilterPipe } from './str-filter.pipe';

describe('StrFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StrFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
