import { ActivityOverviewSortingPipe } from './activity-overview-sorting.pipe';

describe('Pipe: ActivityOverviewSortinge', () => {
  it('create an instance', () => {
    let pipe = new ActivityOverviewSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
