/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SyntaxHighlightedTextareaDirective } from './syntax-highlighted-textarea.directive';

describe('Directive: SyntaxHighlightedTextarea', () => {
  it('should create an instance', () => {
    const directive = new SyntaxHighlightedTextareaDirective();
    expect(directive).toBeTruthy();
  });
});
